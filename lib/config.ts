// Points at the product while it's only running locally. Swap both once
// Sevenz is deployed — nothing else in this landing needs to change.
export const APP_URL = "http://localhost:3000";
export const SIGNUP_URL = `${APP_URL}/signup`;
export const LOGIN_URL = `${APP_URL}/login`;
